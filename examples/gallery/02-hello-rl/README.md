# Hello RL

## What
Run a tiny GRPO round with the reusable recipe layer on the fake provider.

## Why
This demonstrates the live-sampling, reward, advantage, and policy-loss helpers without touching the older example scripts.

## Expected Output
`run.py --smoke` prints one round of mean reward, shows finite losses, writes a checkpoint, and samples from the saved adapter.

## Cost
$0
